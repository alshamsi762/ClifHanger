const Global = require('gameStageGlobal.js');

test('Check the Global variable 'gameArea' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'renderer' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'camera' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'controls' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'scene' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'axesHelper' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'ambLight' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'camLight' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'cubeMaterial' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'plateMaterial' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'cubeGeometry' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'plateGeometry' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'cube' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});

test('Check the Global variable 'plate' is what is expected', () => {
  expect(Global.gameArea).toBe(value);
});
